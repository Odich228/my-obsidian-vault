```
(обновляемся)
apt-get update
apt-get dist-update
update-kernel
reboot

(Работа с редакцией)
alteratorctl editions (Смотрим нащу редакцию)
alteratorctl editions set edition_domain (Меняем нашу редакцию)

(Установка необходимых пакетов)
apt-get install alterator-datetime alterator-fbi alterator-net-eth (пакеты для настройки параметров через web-цус, можно обойтись без них)

Ставим ДНС
пока ставьте 8.8.8.8 для скачки пакетов 

Скачка
alteratorctl components install samba-dc (почему не через apt-get?)

Устраняем возможные конфликты
systemctl stop smb nmb krb5kdc slapd bind dnsmasq
systemctl disable smb nmb krb5kdc slapd bind dnsmasq
rm -f /etc/samba/smb.conf
rm -rf /var/lib/samba/
rm -rf /var/cache/samba
mkdir -p /var/lib/samba/sysvol


