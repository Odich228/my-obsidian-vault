становите веб-сервере Apache2 на хост LIN-SRV1

```
apt-get install apache2 apache2-mod_auth_gssapi -y
```

На веб-сервере установить пакет apache2-mod_auth_gssapi и включить необходимые модули:

```
a2enmod auth_gssapi
a2enmod authn_core
a2enmod authz_user
service httpd2 condreload
```
Добавить в конфигурацию Apache строки:

```
<Location "/login.html">
        AuthType GSSAPI
        AuthName "GSSAPI Login"
        #GssapiBasicAuth On
        GssapiCredStore keytab:/etc/httpd2/conf/httpd.keytab
        GssapiAllowedMech krb5
        Require valid-user
</Location>
```




Кeytab файл
Пользователь
```
samba-tool user add --random-password webauth
samba-tool user setexpiry webauth --noexpiry
```
Создаем SPN и keytab файл
```
samba-tool spn add HTTP/web.semifinal.irpo webauth
samba-tool domain exportkeytab /tmp/httpd.keytab --principal=HTTP/web.semifinal.irpo@SEMIFINAL.IRPO
```



Перенести полученный файл keytab на веб-сервер в каталог `/etc/httpd2/conf/`, установить права на него, так чтобы Apache мог читать, но не изменять keytab-файл:
```
chown apache2:apache2 /etc/httpd2/conf/httpd.keytab
chmod 440 /etc/httpd2/conf/httpd.keytab
```



```
echo '<html><body><h1>Authentificated!</h1></body></html>' > /var/www/html/login.html
```
cp /template/login.html /var/www/html/





